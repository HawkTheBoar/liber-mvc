+ add integrity checks to userfactory
+ add the product and category pages (pretty easy)
+ check if deleting works
+ make a route and controller for inserting values into general tables
+ make a quick dashboard